<!DOCTYPE html>
<html lang="en">
<head>
 <title>TinyApp</title>
</head>
<body>

    <!-- Add your EJS code here -->
   

	 
	
	<% if ((user) && user !== null) {%>
	<header><%= user.email %></header>
	 <form method="POST" action="/logout">
      <br></br>
      <button>Logout</button>
      <br></br>
    </form>

	<br>
	<h2>Your URLs:</h2>
	<br>
	<% for (var keys in urls ) {%>
	<% if (user.email) {%>

		<form method="POST" action="/urls/<%=keys%>/delete">
    	<p>
    		  SHORT URL : <a href="/u/<%=keys%>"><%=keys%> </a> ||||  URL : <a href="<%=urls[keys].longURL%>"><%= urls[keys].longURL%></a>
    		 <a href = "/urls/<%=keys%>"> EDIT  </a> 	
			<button>DELETE</button>
    		<br></br>
    	</p>
    	</form>	
	</form> 
	<% }%>
	<% }%>
	
    
    
     <a href="/urls/new"><button>Add a URL:</button></a>
   
	


	<% } else {%>
	 <header>
    <% include ./partials/_header %>
	</header>
	<h3 style ="color:red">PLEASE <a href="/login">LOGIN</a> FIRST</h3>
	<% }%>

   
</body>
</html>